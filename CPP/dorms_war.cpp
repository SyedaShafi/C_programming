#include <bits/stdc++.h>
#define ll long long int
using namespace std;
const int N = 1e7 + 1;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string s;
        cin >> s;
        int k;
        cin >> k;
        char ele;
        vector<pair<char, char>> p;
        vector<char> v;
        for (int i = 0; i < k; i++)
        {
            cin >> ele;
            v.push_back(ele);
        }
    }
}